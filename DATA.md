# Data

## Where data comes from

This project analyzes **real Forex market data**, not Pocket Option's OTC
instruments (which have no official public API, and where any automation —
even just reading your own session — risks account closure; see README).
Two supported sources today:

- **OANDA v20 API (`OandaDataSource`)** — real Forex market data, live and
  historical, for a wide range of granularities (1 minute and above in this
  project — see "Why not 30-second candles" below). Requires a free
  practice (demo) account and a personal access token, read from the
  `OANDA_API_TOKEN` environment variable (never hardcoded, never
  committed). Only candles OANDA itself marks `complete` are ever used —
  the still-forming current candle is always discarded.
- **User-provided CSV files (`CsvDataSource`)** — any historical OHLC export
  you already have (columns: timestamp, open, high, low, close), ingested
  via `scripts/import_csv.py`.

What this project deliberately does **not** do:

- Scrape Pocket Option's website or app, automate login, bypass 2FA/CAPTCHA,
  or reverse-engineer private/internal endpoints.
- Fabricate, simulate, or "reasonably estimate" candles as a stand-in for
  real data. If a candle can't be obtained, the system has nothing for that
  moment — full stop.

If you gain access to another **legitimate, authorized** data feed later,
implement it as a new `DataSource` subclass
(`src/otc_research/data/sources/`) — the rest of the pipeline (validation,
storage, everything built on top) does not need to change.

## Why not 30-second candles

OANDA's API does technically support sub-minute granularity, but this
project's supported timeframes start at 1 minute by deliberate choice (see
SIGNAL_ENGINE.md) — not because the data doesn't exist. If a future
timeframe genuinely can't be obtained at the granularity requested, the
system must say so explicitly rather than interpolating or aggregating
coarser data to fake it.

## What cannot be guaranteed

- That any statistical edge found in real Forex historical data persists
  into the future — this is inherent to trading research, not specific to
  this project.
- That an edge found on real Forex data transfers to Pocket Option's (or
  any other broker's) OTC synthetic instrument — those are generated by a
  different, non-public process and can behave differently, especially on
  weekends when the real market is closed but OTC instruments keep trading.
- Anything about live execution quality (latency, slippage, rejected
  orders) if you choose to act on a signal manually — see BACKTESTING.md's
  execution-simulation scenarios, which exist precisely because
  "realistic" and "optimistic" backtest assumptions can produce very
  different conclusions.

## Database schema

SQLite by default (`config/config.yaml: database.url`), portable to
PostgreSQL by changing the URL — the schema is defined in plain SQLAlchemy
(`src/otc_research/db/models.py`) with no SQLite-specific types.

### `candles`

Raw OHLC data. Nothing derived lives here.

| column | notes |
|---|---|
| asset | e.g. `EUR_USD` |
| timeframe | `1m`, `5m`, `15m`, ... |
| timestamp | candle **open** time, UTC, timezone-aware |
| open/high/low/close | as provided by the source |
| source | provenance string, e.g. `csv:eur_usd_1m.csv` |
| is_synthetic_test_data | must be `False` for anything used in real research; `True` is only ever used by the test suite |

Unique constraint on `(asset, timeframe, timestamp)` — a candle is inserted
once and never overwritten by re-ingestion.

### `data_quality_issues`

Every finding from the validation layer, one row each. Types currently
produced: `duplicate_timestamp`, `out_of_order`, `impossible_value` (all
three block insertion of the offending candle), and `gap`, `suspicious_move`,
`source_change` (all three are informational and do not block insertion of
the surrounding valid candles). Nothing in this table ever triggers
automatic data repair — it's a log for a human to look at.

### `features` (schema in place, not yet populated — Phase 3)

Point-in-time computed values, versioned by `feature_set_version` so that a
change to how a feature is computed doesn't silently invalidate old
backtests — it produces a new version instead. The hard rule for anything
written here: a feature at `timestamp` may only be computed from candles
with `timestamp' <= timestamp`. Anything else is look-ahead bias.

### `hypotheses` (schema in place, seeded with the initial 10 — see STRATEGIES.md)

Every strategy idea considered, registered before its results are known.
This exists to make multiple-testing / data-mining bias auditable: an
outside reader can see how many hypotheses were tried in total, not just
whichever one is being reported on.

### `signals` (schema in place, not yet populated — Phase 9)

A generated signal and, once resolved, its outcome — see SIGNAL_ENGINE.md
for the full design. Write-once for the inputs that produced it
(`features_snapshot`, `conditions_met`, `score`, `historical_sample_size`,
`historical_win_rate`, `win_rate_ci_low/high`, `expectancy`, ...); only the
`result`/`pnl`/`expiry_price` fields are filled in later, after the target
horizon elapses and the real outcome is fetched. `payout` is nullable — a
plain directional Forex signal has none; it's only relevant if you
manually place it as a fixed-payout instrument elsewhere.

## Data validation rules (Phase 2)

Implemented in `src/otc_research/data/validation.py`, run by
`src/otc_research/data/ingestion.py` on every ingestion batch:

- **Duplicate timestamps** — rejected (candle not inserted).
- **Out-of-order timestamps** — rejected.
- **Impossible OHLC values** (`high < low`, high/low not bounding open/close,
  non-positive prices) — rejected.
- **Gaps** (missing bars implied by the timeframe) — logged, surrounding
  valid candles still inserted, nothing synthesized to fill the gap.
- **Suspicious moves** (close-to-close percentage move above a configurable
  threshold, `data_validation.max_suspicious_move_pct` in config.yaml) —
  logged only; real Forex data can legitimately gap/jump, especially around
  news events or market open.
- **Source changes** (new data for an asset/timeframe arriving from a
  different `DataSource.name` than what's already stored) — logged so a
  provenance change mid-history is auditable.
- **Already-ingested candles** (same asset/timeframe/timestamp already in
  the DB) — skipped, not overwritten.

None of these rules fabricate data. A rejected or missing candle simply does
not exist in the database.
